# =====================================================
# EVA Note MVP - Environment Variables
# =====================================================

# -----------------------------------------------------
# Clerk Authentication (EU tenant required)
# -----------------------------------------------------
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Clerk webhook secret (for Supabase Edge Function sync)
CLERK_WEBHOOK_SECRET=

# -----------------------------------------------------
# Supabase (EU region required for GDPR compliance)
# -----------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# -----------------------------------------------------
# Azure OpenAI (EU region REQUIRED - will fail startup if not EU)
# Valid EU regions: westeurope, germanywestcentral, francecentral,
# northeurope, switzerlandnorth, norwayeast, swedencentral, polandcentral
# -----------------------------------------------------
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_REGION=germanywestcentral
AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini-eu
AZURE_OPENAI_API_VERSION=2024-02-01
LLM_MAX_OUTPUT_TOKENS=1024

# -----------------------------------------------------
# Deepgram Speech-to-Text (streaming transcription)
# -----------------------------------------------------
DEEPGRAM_API_KEY=
DEEPGRAM_MODEL=nova-3
DEEPGRAM_LANGUAGE=de

# -----------------------------------------------------
# WebSocket Configuration (Deepgram streaming)
# -----------------------------------------------------
WS_TIMEOUT_MS=30000
WS_HEARTBEAT_INTERVAL_MS=10000
STT_MAX_DURATION_SEC=900

# -----------------------------------------------------
# Cost Tracking (euro cents, indicative 2025 rates)
# Update based on actual pricing from providers
# -----------------------------------------------------
# Deepgram Nova-3 (pay-as-you-go, cents EUR/second)
STT_RATE_CENTS_PER_SEC=0.16

# Azure OpenAI gpt-4o-mini (cents EUR per 1K tokens)
LLM_IN_CENTS_PER_1K=0.013
LLM_OUT_CENTS_PER_1K=0.052

# -----------------------------------------------------
# Application
# -----------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------
# Monitoring (Optional - post-MVP)
# -----------------------------------------------------
# SENTRY_DSN=
# SENTRY_AUTH_TOKEN=
# NEXT_PUBLIC_POSTHOG_KEY=
# NEXT_PUBLIC_POSTHOG_HOST=
